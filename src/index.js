import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import RoutesBFood from './routes/Routes';

ReactDOM.render(
  <React.StrictMode>
    <RoutesBFood />
  </React.StrictMode>,
  document.getElementById('root')
);
